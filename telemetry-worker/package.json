{
  "name": "lander-ntp-telemetry",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "wrangler types && tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "valibot": "^1.5.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^7.3.6",
    "vitest": "^5.0.1",
    "wrangler": "^4.134.0"
  }
}
